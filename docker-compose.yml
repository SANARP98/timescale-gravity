version: "3.9"

services:
  db:
    image: timescale/timescaledb:latest-pg14
    container_name: timescale-gravity-db
    environment:
      POSTGRES_DB: trading
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
      - ./db_setup.sql:/docker-entrypoint-initdb.d/001-db-setup.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d trading"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  app:
    build: .
    container_name: timescale-gravity-app
    depends_on:
      db:
        condition: service_healthy
      cloudbeaver:
        condition: service_started
    env_file:
      - .env
    environment:
      PGHOST: db
      PGPORT: "5432"
      PGUSER: postgres
      PGPASSWORD: postgres
      PGDATABASE: trading
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    restart: unless-stopped
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]

  cloudbeaver:
    image: dbeaver/cloudbeaver:latest
    container_name: timescale-gravity-cloudbeaver
    depends_on:
      db:
        condition: service_healthy
    environment:
      CB_ADMIN_NAME: admin
      CB_ADMIN_PASSWORD: admin
    ports:
      - "8978:8978"
    volumes:
      - cloudbeaver_workspace:/opt/cloudbeaver/workspace
    restart: unless-stopped

volumes:
  db_data:
  cloudbeaver_workspace:
